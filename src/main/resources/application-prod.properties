# Production Environment Configuration

# GitHub and GitLab API authentication tokens should be provided as environment variables
# git.auth.tokens.github=${GITHUB_API_TOKEN}
# git.auth.tokens.gitlab=${GITLAB_API_TOKEN}

# Disable debug logging
logging.level.com.gitintegration=INFO
logging.level.org.springframework.web=INFO

# Optimize for production
spring.jackson.serialization.indent-output=false

# Server configuration
server.tomcat.max-threads=100
server.tomcat.min-spare-threads=20